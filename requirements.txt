# Deploy / viewer dependencies — what the hosted dashboard + Iris chat need.
# Deliberately slim so Streamlit Community Cloud installs fast and reliably
# (the ML stack — torch, scikit-learn, sentence-transformers — is heavy and
# only needed to RUN the pipeline / upload feature; see requirements-pipeline.txt).

pandas>=2.2
pyarrow>=16.0
numpy>=1.26

# App
streamlit>=1.36
plotly>=5.22

# Config / infra
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
PyYAML>=6.0
tenacity>=8.3
diskcache>=5.6

# LLM providers (Iris chat + synthesis). Lightweight SDKs.
groq>=0.11
google-genai>=1.0
